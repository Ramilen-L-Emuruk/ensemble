# CLAUDE.md

MultiTrackPlayer（マルチトラック対応 Windows 動画プレイヤー）で作業するときの手順とルール。

## プロジェクト概要

- C# + WPF (.NET 10) 製の Windows 動画プレイヤー。
- 映像エンジン: Sdcb.FFmpeg（D3D11VA ハードウェアデコード対応）。
- 音声: NAudio (WASAPI)。マルチトラック音声の同時再生・個別音量制御に対応。
- MVVM: CommunityToolkit.Mvvm。
- ソリューション構成: Core（モデル・インターフェース）/ Engine（FFmpeg + NAudio）/ UI（WPF）。

## 変更時の基本フロー（必ずこの順で行う）

1. **状況整理・修正方針の確認**（実装に着手する前に必ず行う。自明な軽微修正でも省略しない）
   - 対象のコード・挙動を調査し、現状と問題点（原因・影響範囲）を整理する
   - **被害の見積もりは、方針を提示する前に裏取りする。** このプロジェクトは並行処理の不具合が
     大半で（スレッド待ち合わせ・GPU/CPU 二経路・3 つの滞留検出）、被害の見積もりを外すと
     直す場所ごと変わる。やることは 2 つ。
     - **「同時に呼ばれる」と書く前に、呼び出し元を全部数える。** 経路が排他かどうか・過渡状態で
       重なるかまで追う（数え落とすと規模も対策も見当違いになる）
     - **「こう壊れる」と書く前に壊してみる。** ただし**`dotnet test` で再現できる範囲に限る**
       （ロックやガードを一時的に外し、テストが落ちるかを見る）。**外す前に対象ファイルを
       別名で控え、上書きで戻す**——手で書き戻すと、セッションが中断したときに外した状態が残る。
       **アプリ全体の起動を要する検証は自分で行わない**——下記「検証」のとおりユーザーへ依頼する。
       テストで再現できないなら**断定せず「未確認の仮説」として提示する**
   - **`.claude/REVIEW-REMEDIATION-STATUS.md` に積んである見積もりは仮説として扱い、着手時に
     再検証する。** 積んだ時点の理解が古い・誤っていることがある（構造上の問題自体は実在するので
     作業は無駄にならないが、被害の説明が違えば対策の形が変わる）
     - **このファイルは gitignore 対象で、ワークツリーには存在しない。** 参照・更新はメインの
       チェックアウト側で行う。手順 2 でワークツリーを切る前に読んでおくこと
   - 整理した内容と修正方針（どこを・なぜ・どう直すか）をユーザーに提示する
   - ユーザーの確認を得てから次のステップに進む。指摘や方針変更があれば整理からやり直す
   - **複数の段・フェーズに分かれる作業は、着手前にタスクとして登録する**（下記「作業の記憶と切り出し」）
   - **ここで合意した範囲・深さ・粒度（「全て」等）を、後から自分の判断で狭めない。** 縮小が必要になったら実装の途中でも一旦手を止め、何をどこまで削るかを提示して確認を得る。**縮小した状態を先に成果物として出さない**
2. **ワークツリー作成**（修正・機能追加時はワークツリーを作成してから作業する。ブランチ名は必ず `worktree/<type>/<name>` 形式にする: `worktree/fix/〇〇`・`worktree/feat/〇〇`・`worktree/refactor/〇〇`・`worktree/docs/〇〇`・`worktree/chore/〇〇` など）
   - **Step 0 の `git pull` を飛ばさないこと。** ローカルの `main` が `origin/main` に遅れたまま切ると、後退した地点から作業を始めることになり、あとで作り直す手戻りになる
   - **注意**: `EnterWorktree(name: ...)` はブランチ名を自動変換してしまうため使用しない。必ず以下の手順で行う:
     ```bash
     # Step 0: メインの checkout 側でローカル main を最新へ
     git pull
     # Step 1: 正しいブランチ名でワークツリーを作成
     git worktree add -b worktree/<type>/<name> .claude/worktrees/<name>
     # Step 2: 作成したワークツリーに入る
     EnterWorktree(path: ".claude/worktrees/<name>")
     ```
3. **実装**（ワークツリー内ではバージョンフィールドを変更しない。理由は下記「バージョン管理」参照）
   - 実装中・実装後に [.claude/rules/ensemble-review.md](.claude/rules/ensemble-review.md) のチェックリストを突き合わせる。**過去に実際の不具合を出した箇所**（スレッド待ち合わせの取りこぼし・GPU/CPU 二経路の非対称・ネイティブリソースの解放順序・キー入力経路の分散）をまとめてある
4. **検証**（下記「検証」を実施。ビルドは必須。**実行確認はユーザーが行うため、exe のパスと確認手順をユーザーに連携する**）
5. **敵対的レビュー**（下記「敵対的レビュー」の手順で必ず実施。**全変更が対象。小規模修正でも省略しない**。指摘があれば実装に戻る。**ただし無条件に繰り返さない**——打ち切り条件は下記「巡回をどこで打ち切るか」）
6. **コミット前確認**（実装・検証・敵対的レビューが終わったら、コミットする前に必ずユーザーに確認を取る。**省略しない**）
   - 何を・なぜ・どう直したかを簡潔に提示し、コミットしてよいか確認する
   - 確認が取れるまで次のステップに進まない
7. **コミット**（確認が取れたら、下記「コミット」の規約に従いコミットする）
8. **`main` の取り込み**（次の手順で改名する直前に 1 回。ワークツリー内で行う）
   - `git merge main` で先行分を取り込む。改名した時点の内容がそのままリリース対象になるため、ここで最新に揃えておく
   - コンフリクトを解消したら検証を再実行する。**解消が実質的なコード変更を伴った場合は敵対的レビューをもう 1 度回す**
9. **`ready/*` へ改名**（**改名前に必ずユーザーに確認を取る**。改名は「この内容でリリースしてよい」という宣言にあたる）
   - 確認は **AskUserQuestion** で 4 択（リリース待ちのみ／リリース待ち＋片付け／続けてリリース／まだ改名しない）
   - `git branch -m worktree/<type>/<name> ready/<type>/<name>`
   - **ここで実装セッションは終了。** マージ・バージョン更新・プッシュを自発的に行わない
   - 選択肢・省略条件の詳細はグローバルの `rules/common/worktree.md`「完了時の改名」を参照
10. **リリース**（**ユーザーから明示的に指示があったときのみ**）
   - `release` スキルを使う（全 8 手順）。`ready/*` の列挙 → **出すものをユーザーに選ばせる** → マージ（`--no-ff`）→ 統合後の検証 → バージョン更新 → プッシュ → クリーンアップ → **完了後の再確認**
   - 最後の「完了後の再確認」は、他セッションが新たな `ready/*` を積んでいないかを見る手順。**黙って終わらせない**ためのものなので省略しない
   - このプロジェクトで読み替えが必要な手順は下記「リリース（exe 発行）」を参照

> 8〜9 と 10 を分けている理由: 複数セッションが並行して作業すると `main` が奪い合いになる。`main` を書き換える操作をリリース手続きに集約し、その主体を常に 1 つに限ることで、実装セッション間の順番調整が不要になる。

## 検証

> **実行確認はユーザーの環境で行う。Claude 自身は `dotnet run` でアプリを起動しない。**
> ビルドが通ったら、実行 exe の path と、確認してほしい操作手順をユーザーに連携する。

- **ビルド（必須）**: `dotnet build`。エラー 0 を確認する。
- **大きめの変更時**: Release ビルドも確認: `dotnet build -c Release`。
- **実行確認の連携（必須）**: ビルド後、以下をユーザーに提示する。
  - **実行 exe の path**: 必ず絶対パスで連携する（相対パスだと、どのワークツリー／チェックアウトの exe か伝わらず、古い exe を誤って実行される事故につながる）。コピーしやすいよう独立したコードブロックで示す。
    - パスは固定部分と、下表の**可変部分 3 つ**を繋いだ形になる。**組み合わせを例として書き起こさない**（下記の理由）。
      ```
      <ビルドしたチェックアウトの絶対パス>\src\MultiTrackPlayer.UI\bin\<構成>\<TFM>\MultiTrackPlayer.UI.exe
      ```
      | 部分 | 決まり方 |
      |---|---|
      | チェックアウト | ワークツリー内でビルドしたなら `<リポジトリ>\.claude\worktrees\<name>`、main 直下なら `<リポジトリ>`。リリースの統合後検証は `release` スキルが main 上での実行を要求するため必ず後者 |
      | 構成 | `Debug` / `Release`。実行したビルドコマンドどおり |
      | TFM | 単一の情報源は `src/MultiTrackPlayer.UI/MultiTrackPlayer.UI.csproj` の `<TargetFramework>` |
    - **可変部分は例に書き起こさないこと。** リポジトリの置き場所は環境で違い、TFM は移行のたびに変わり、構成とチェックアウトは作業のたびに変わる。書き起こせば必ずどれかが腐り、組み合わせを列挙すれば必ずどれかが抜ける。**毎回、実際の出力先を確認してから組み立てること。** 古いパスを提示すると、存在しない exe や別バージョンの exe をユーザーに実行させる事故になる。
  - **確認してほしい手順**: 何を・どう操作すると・どうなれば正常かを具体的な操作ステップで示す（曖昧に「動作確認してください」で済ませない）
  - ユーザーからの確認結果（動いた／動かなかった）を待ってから次のステップ（敵対的レビュー）に進む

## 敵対的レビュー

**全変更（コード・ドキュメント問わず）でエージェントによる敵対的レビューを実施する。** 実装から裏取りしたバグ・エッジケース・機能間競合を検出し、コミット前に「レビュー観点の抜け」を客観的に潰す。

エージェント構成・統一フォーマット・メタレビューの詳細はグローバルの `rules/common/adversarial-review.md` を参照。

**このプロジェクトには `.claude/agents/` が無く、レビュアーは汎用の定義で起動する。** そのため固有の観点は自動では効かない——**起動時のプロンプトに [.claude/rules/ensemble-review.md](.claude/rules/ensemble-review.md) を読むよう必ず書くこと。** ここには過去に実際の不具合を出した領域（スレッド待ち合わせの取りこぼし・GPU/CPU 二経路の非対称・ネイティブリソースの解放順序・キー入力経路の分散）が、原因コミット付きでまとめてある。渡し忘れると、**最も再発しているクラスの不具合がレビューから丸ごと抜ける。**

### 実施タイミング

**検証（ビルドとユーザーによる実行確認）が完了した後、コミット前確認の前**。指摘があれば実装に戻り、修正後に敵対的レビューをもう 1 度回す。
**ただし無条件に繰り返すわけではない**（下記「巡回をどこで打ち切るか」）。

### 巡回をどこで打ち切るか

**「指摘が出たら直してもう 1 巡」を無条件に続けると終わらない。** 修正が次の指摘を産む連鎖が起き、重要度も下がらないまま巡数だけが伸びる。**打ち切りの判断は Claude 側の義務**——ユーザーが「これで十分」と言うのを待つのは、収束していないことに気づく責任をユーザーへ押し付けている。

**「1 巡」= レビューを 1 回実行してから、指摘を反映して次のレビューを実行する直前まで。** **並列のエージェントは何本でも合わせて 1 巡**。メタレビューも同じ巡に含める。

**CRITICAL は巡数によらず解消する。** CRITICAL が 4 巡目・5 巡目に出たなら、それは「継ぎ足しでは直らない」という兆候なので、**修正を重ねるのをやめて設計を見直す**——巡回を終える口実にはしない。打ち切ってよいのは HIGH 以下で、HIGH を残す場合は**残す判断をユーザーに確認する**（勝手に格下げしない）。

**比較するのは各巡の「最高重要度」だけ。** 件数の増減や MEDIUM / LOW の増減は判定に含めない（1 巡目 HIGH 2 件 → 2 巡目 HIGH 1 件＋MEDIUM 3 件は「下がっていない」）。曖昧な基準にすると「もう十分」と言いたい方向へ判断が寄る。

| 状況 | 判断 |
|---|---|
| 最高重要度が巡ごとに下がっている（HIGH → MEDIUM → LOW） | 収束している。そのまま続けてよい |
| **3 巡を超えても最高重要度が下がらない** | **修正を重ねるのをやめる。** 設計そのものを見直すか、`.claude/REVIEW-REMEDIATION-STATUS.md` へ積んで区切る（CRITICAL は上記のとおり打ち切れない） |
| 指摘が「前の巡で入れた修正の文面」だけになった | 実装の指摘が 1 件でも混じっているならまだ続ける。文面だけなら、まとめて直して締める |
| 同じ箇所を 3 巡以上書き換えている | その箇所の設計が問題。継ぎ足しをやめて作り直しを検討する |

打ち切るときは**何を残したかをユーザーへ明示し、`.claude/REVIEW-REMEDIATION-STATUS.md` へ積む**。黙って終わらせない。「巡回を打ち切った」と「指摘が無くなった」は別の状態で、混ぜると次に触る人が誤解する。

### レビュー観点（全エージェント共通）

- **実装から裏取り**: 既存コメント・CLAUDE.md を鵜呑みにせず、実コードを読んで挙動を確認する
- **修正はしない**: レビュアーは変更をコミットしない。指摘のみ返す
- **前の巡の修正を、元のコードと同じ厳しさで見る**: 指摘を潰すために足した抑制・ガード・早期 return が、**指摘の範囲より広い範囲を止めていないか**を確かめる。過剰補正は元の問題より重くなりうる
- **矛盾しているのは実装か文面かを見分ける**: 記録・通知の文面が事実以上を主張しているだけなら、**直すのは文面**。ロジックを止めて黙らせると、その検出機能ごと失う
- **分類**: CRITICAL / HIGH / MEDIUM / LOW で分類する

## プランモード

- プランモードに入ったとき、前回のプランが**完了済み**（実装・コミット済み）の場合は、既存プランファイルを修正せず**新規プランファイルを作成**する。
- 前回プランが未完了（作業途中）の場合のみ、既存プランファイルを引き続き更新してよい。

## 作業の記憶と切り出し

コンテキストは失われる。**会話へ書いただけの計画は、失われた時点で存在しなくなる。**
この節は「思い出せる形で残す」ための決まり。

> ここでいう**チップ**とは、`spawn_task` で作る「別セッションへの申し送り」のこと。
> ユーザーが 1 クリックで別セッションとして開始できる（扱いは下記「作業中に別の課題を見つけたとき」）。

### 進める作業はタスク機能で管理する

**複数の段・フェーズに分かれる作業は、着手前に `TaskCreate` で登録する。**

- **依存関係を宣言する**。`TaskUpdate` の `addBlockedBy` / `addBlocks` で「A が終わるまで B は着手できない」を表すと、`TaskList` が blocked として返す。順序を取り違えない
- 状態は `pending` → `in_progress` → `completed`。**着手時に `in_progress`、完了時に `completed`**。部分的にしか終わっていないものを `completed` にしない（テストが落ちている・実装が途中・必要なファイルが見つからない、のいずれも完了ではない）
- **単一で些末な作業には使わない**（`TaskCreate` 自身がそう定めている）。目安は段・フェーズに分かれるか、3 ステップ以上か
- **タスク機能やチップ（`spawn_task`）はハーネスが提供するもので、どの環境にもあるとは限らない。**使えない配布先では、この節が求める記録を資材（リポジトリ内のファイル）への直接記述だけで満たす
  - 進捗管理の道具は環境によって名前が違う（`TaskCreate` 系のほか、`TodoWrite` を持つ環境もある）。**ここで定めているのは道具ではなく記録の要件**——依存関係を宣言し、着手と完了を区別し、消えては困るものを資材へ残すこと。手元にある道具でこれを満たせばよい
- **消えては困る計画は資材にも書く。** タスクリストはセッション用で、永続する保証がない。手順・判断基準はリポジトリ内の資材（`CLAUDE.md`・仕様書配下）へ置く。Claude のメモリは端末ごとに閉じているため代わりにならない

**会話へ書いただけの計画は、セッションが再起動した時点で自分でも思い出せなくなる。** ユーザーから見れば「決めたはずのことを忘れている」状態で、指摘されるまで気づけない。

### 作業中に別の課題を見つけたとき

**まず現在の変更と競合するかを判定する。** 競合するなら同じセッションで直すことを提案し、独立なら `spawn_task` でチップに切り出す。

競合と見なす条件（どちらかを満たせば競合）:

- **同じファイルを触る**（同じ関数である必要はない。対になる関数や近接する処理も含む）
- **前段の結果でその課題の方針が変わる**（直列の依存がある）

| 例 | 判定 | 扱い |
|---|---|---|
| いま直している関数と対になる関数に同じ欠陥がある | 同じファイル → 競合 | 同じセッションで直す |
| 別ファイルにあるコメント漏れ・命名の不統一 | 独立 | チップ |
| 前段の調査結果を受けて方針が決まる複数の段 | 直列依存 → 競合 | **1 本のチップにまとめる** |

**チップ同士が競合する場合は 1 本にまとめる。** 1 クリックで別セッションが始まるため、依存関係のあるものを並べると同時に押せてしまう。プロンプトに「前提」と書いても、**それを守る責任をユーザーへ押し付けるだけ**で、依存そのものは表現できない。依存を表現したいなら `TaskUpdate` の `addBlockedBy` を使う——チップにその機能は無い。

**自分でそのチップの内容に着手したら、着手した時点で `dismiss_task` で取り下げる**（完了時ではない）。残したままだとユーザーが押し、別セッションが同じ修正を並行して始める。

### 会話の内容を思い出せないときはトランスクリプトを読む

コンテキストが失われて「前に何を決めたか」が分からなくなったら、**推測で進めず記録を読む**。

```
<ホームディレクトリ>/.claude/projects/<プロジェクトのパスをハイフンで繋いだ名前>/<sessionId>.jsonl
```

- `sessionId` は環境変数 `CLAUDE_CODE_SESSION_ID` から取れる。**まずこれを見ること**——追加のツールを必要としない
  - `mcp__ccd_session_mgmt__get_session` に `"self"` を渡すとタイトルも併せて分かるが、この MCP が入っていない環境では使えない
  - **どちらも使えないときは、対象プロジェクトのディレクトリにある `.jsonl` を更新時刻の新しい順に見て候補を絞る。** `sessionId` が分からないことは、読む手段が無いことを意味しない
  - **ただし「一番新しい」は「一番最近動いた」であって「自分」ではない。** 同じプロジェクトで
    別セッションが並行していれば、そちらを掴む。**採用する前に、中身が自分の文脈と合うかを
    確かめること**（作業ディレクトリ・直近に触ったブランチ名・時刻の連なり）。
    決め手が無ければ要点をユーザーへ提示して確認を取る。他人の会話を「自分が決めたこと」として
    取り込むと、以後の判断がまるごとずれる
- JSONL 形式（1 行 1 メッセージ）なので grep できる。再起動前の分が `.jsonl.pre-import` に残ることもある
- **`list_events` と `search_session_transcripts` は自分のセッションを対象にできない。** 自分の記録はファイルを直接読むしかない。ここはハーネス側の仕様なので、動かなくなったらまず挙動が変わっていないかを疑うこと

**「復元できない」と決めつけて調査をやり直さないこと。** 記録は最初からファイルとして存在する。「無い」と判断する前に、読む手段があるかを確かめる。

## コミット

- **Conventional Commits**（`feat` / `fix` / `refactor` / `docs` / `chore` / `perf` / `ci`）。説明は日本語。
- コミットメッセージ末尾に必ず付与する。**モデル名はその時作業している Claude モデルに合わせる**（ハーネスが指定するモデル名を使用する。特定モデルに固定しない）:
  ```
  Co-Authored-By: Claude <モデル名> <noreply@anthropic.com>
  ```
  例（Sonnet 5 で作業時）: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`
- Windows 環境のため `LF will be replaced by CRLF` の警告が出るが正常（無視してよい）。
- ワークツリー内のコミットではバージョンフィールドを変更しない（下記「バージョン管理」参照）。
- **コミット前に必ずユーザーに確認を取る。省略しない**。同一セッション内で別の修正のコミット許可をもらっていても、その修正のコミット許可を個別にもらっていない場合は確認する。

## マージ

**`main` へのマージは `release` スキルの手順 3 でのみ行う。実装フローの途中では行わない。**

- ファストフォワード可能な場合でも、必ずマージコミットを作成する（`git merge --no-ff`）
  - 理由: 各機能・修正の単位（ブランチ）を履歴上で明確に残すため
- 実装フローの手順 8 で行う `git merge main` は逆方向（先行分の取り込み）であって、これとは別物

## バージョン管理

`src/MultiTrackPlayer.UI/MultiTrackPlayer.UI.csproj` の `<Version>` タグでバージョン番号を管理している。ここがバージョンの単一の情報源。

**バージョンを上げるのは `release` スキルの手順 5（統合後の検証を通した後・main 上）だけ。** ワークツリー内やマージ前のブランチではバージョンフィールドを変更しない。上げ幅の判断とユーザーへの確認はスキル側の手続きに従う。

- **ワークツリーで触らない理由**: 複数のワークツリーを並行して進めている場合、各セッションがワークツリー作成時点の（まだ更新されていない）古いバージョンを見て同じ番号を選んでしまい、結果的にバージョンが正しく積み上がらない事故が起きる（例: 1.0.0 から2つのセッションが同時にパッチ修正すると、両方とも 1.0.1 を選んでしまい最終的に 1.0.1 のままになる）。リリースは 1 つの主体が順番に処理するため、その中でバージョンを決めれば常に最新の値を見て +1 できる。
- バージョン表示箇所が複数ある場合は、可能な限り**単一の情報源**（`.csproj` の `<Version>`）から参照する形にし、手動での二重更新を避ける。
- タグベースのリリース（下記「リリース（exe 発行）」参照）では、CI 側が `git tag` の値を `/p:Version=` で注入して上書きする。そのため **タグ名と `.csproj` の `<Version>` は一致させておく**（ズレると exe のファイルバージョン表示と `.csproj` 上の値が食い違う）。

## リリース（exe 発行）

`release` スキルの手続きのうち、このプロジェクトで読み替えが必要なものだけをここで定める。
**実行条件（ユーザーから明示的に指示があったときのみ）は上記「変更時の基本フロー」手順 10 を参照。**
表に出てこない手順 1・2・8（候補の列挙・ユーザーによる選択・完了後の再確認）は汎用のままで読み替え不要。

| スキルの手順 | このプロジェクトでの読み替え・注意点 |
|---|---|
| 3. マージ | コンフリクトを解消した場合の「静的検証」は `dotnet build` でエラー 0 を確認すること |
| 4. 統合後の検証 | `dotnet build -c Release` でエラー 0 ＋ `dotnet test` を通す。そのうえで上記「検証」のとおり **exe の絶対パスと操作手順をユーザーへ連携し、実行確認の結果を待つ** |
| 5. バージョン更新 | `src/MultiTrackPlayer.UI/MultiTrackPlayer.UI.csproj` の `<Version>` を更新してコミットし、**同じ値のタグを打つ**（`git tag -a v1.2.3 -m "v1.2.3"`） |
| 6. プッシュ | `git push --follow-tags` で main とタグをまとめて push する。**main とタグを別コマンドで押さない**（理由は下記） |
| 7. クリーンアップ | ワークツリーとブランチの削除に加え、**ローカル publish**（下記）も行う |

**手順 6 で分けない理由**: スキルのトラブルシューティングは「タグの有無で push 失敗時にどこまで進んでいたか」を判別する前提で組まれている。別コマンドで押すと到達点が曖昧になり切り分けができない。

**ただしこのプロジェクトではタグの有無だけで判別しきれない。** スキル本文が例外として挙げている「バージョンファイルの編集・コミット・タグ付けが別操作になるスタック」に該当するため（`.csproj` を編集 → コミット → `git tag` の 3 操作）、**タグが無くても統合後の検証は済んでいることがある**。`[ahead N]` の状態から再開するときは、**`.csproj` の `<Version>` を変更したコミットがあるかどうか**で判断する。スキルは手順 4（統合後の検証）を通してから手順 5（バージョン更新）へ進む順序なので、そのコミットが存在すれば検証は完了している。

タグを push すると **GitHub Actions（`.github/workflows/release.yml`）が自動実行され、GitHub Release が作られる。**
トリガーは 2 つ。

- **タグ push**: `v` から始まるタグ（例 `v1.0.0`）。上記の手順 5〜6 がこれに当たる
- **手動実行**: GitHub の Actions タブから「Release」ワークフローを選び、バージョン番号を入力して `Run workflow`（スキルの手続きを通さず単発で出したい場合の逃げ道）

- ワークフローの内容: `windows-latest` ランナーで `dotnet publish -c Release -o publish` を実行し、タグ名（または手動実行時の入力値）を `/p:Version=` で注入 → `publish/` フォルダ一式を zip 化し SHA-256 のチェックサムファイルを併せて生成 → GitHub Release を作成しリリースノートを自動生成した上で **zip と `.sha256` の 2 ファイルを添付する**
- **exe 単体では動作しない**。FFmpeg のネイティブ DLL（`runtimes/win-x64/native/avcodec-60.dll` 等）や NAudio・CommunityToolkit.Mvvm の依存 DLL が必要なため、`publish/` フォルダ一式を zip 化して配布する形にしている（single-file 化は未対応）

### リリース時に必ず行うこと: ローカル publish

**タグ push（＝GitHub Actions 経由のリリース）を行った際は、それだけで終わらせず、必ずローカルでも同じ `dotnet publish` を実行すること。** GitHub Actions 側のビルドはクラウド上で完結し、ユーザーの手元の実行ファイルには一切反映されない。ユーザーがすぐに手元で動作確認できるよう、ローカルにも発行物を用意するのが release フローの一部。

```bash
dotnet publish src/MultiTrackPlayer.UI/MultiTrackPlayer.UI.csproj -c Release -o publish
```

- 出力先はリポジトリルート直下の `publish/` に固定する（`-o publish`）。`bin/Release/net10.0-windows/...` のようなバージョン・ランタイム別のネストしたパスには出力しない。
- `publish/` は `.gitignore` 済み（コミット対象外）。GitHub Release は作られない（ローカル確認専用）。
- リリースを伴わずローカルの発行物だけ欲しい場合も、同じコマンドをそのまま使ってよい。

## コメント・ドキュメント整合性チェック

コードを変更した際は、以下の照合を必ず行う。変更が小さくても省略しない。

### 変更前後に確認すること
- **数値・閾値の一致**: コメントに書かれた秒数・件数・スケール値・インデックス範囲が実コードの値と一致するか。
- **列挙の網羅性**: コメントが列挙する要素（トラック種別・レイヤー数・ペイン名など）が実装のすべての要素を網羅しているか。
- **UI 説明文と実データの照合**: ユーザーに表示する説明文が、それが参照するデータの実値と一致するか。

### コメント腐敗を防ぐパターン
- 処理の「単位」「対象範囲」「条件」を書いたコメントは、実装変更時に必ず追従させる。
- 「A または B」「A の場合のみ」のような条件を述べるコメントは、実コードの条件式と突き合わせて確認する。
- 古い識別子・旧実装の痕跡（クラス名・変数名・関数名）がコメントに残っていないか確認する。

### ドキュメントのプロジェクト構成ツリー
- `README.md` にプロジェクト構成ツリーがある場合、新ファイルを追加した際は必ず追記する。
- 削除・リネームしたファイルも同様に更新する。

## 補助コマンド

| コマンド | 用途 |
|----------|------|
| `dotnet build` | ビルド（全プロジェクト） |
| `dotnet build -c Release` | リリースビルド |
| `dotnet run --project src/MultiTrackPlayer.UI` | アプリ起動（**ユーザーが実行確認する際に使うコマンド**。Claude はこれを実行しない） |
| `dotnet test tests/MultiTrackPlayer.Tests/MultiTrackPlayer.Tests.csproj` | テスト実行（xUnit） |
| `dotnet publish src/MultiTrackPlayer.UI/MultiTrackPlayer.UI.csproj -c Release -o publish` | exe 発行（ローカル確認用。GitHub Release は作られない。`publish/` 直下にフラット出力） |

> リリース（main へのマージ・バージョン更新・タグ・push）のコマンドはここに載せない。**`release` スキルの手続きを通すこと**が前提で、単独のコマンドとして書くと手続きを飛ばして実行されうる。内容は上記「リリース（exe 発行）」を参照。

## 構成メモ

- `src/MultiTrackPlayer.Core/`: モデル・インターフェース（依存なし）。ViewModel から切り出した
  純ロジック（`Models/PlaylistCursor` 等）もここに置く。理由は
  [.claude/rules/ensemble-review.md](.claude/rules/ensemble-review.md) の
  「5. テスト可能性の設計」を参照
- `src/MultiTrackPlayer.Engine/`: FFmpeg デコード・NAudio ミキサー（unsafe コード有）
  - `Pipeline/`: ffplay 型スレッド分離パイプライン。`DemuxThread` が `AVFormatContext` を唯一専有し、
    `VideoDecodeThread`/`AudioDecodeThread` が `VideoPacketQueue`/`AudioPacketQueue`（シーク世代・Flush/EOF
    番兵付き有界キュー）経由でデコードする。シークは `DemuxThread.RequestSeek` でコアレスされ非同期処理される。
    映像の書き込み戦略は sink（`IVideoFrameSink`）に委譲し、HW デコード時は `GpuFrameSink`（GPU 色変換）、
    SW デコード時は `CpuFrameSink`（`sws_scale`）を使う
  - `Decoding/VideoDecoder.cs`: 既定デコーダが D3D11VA 非対応なら、同一 codec_id の D3D11VA 対応デコーダ
    （AV1 なら内蔵 `av1`）を `avcodec_get_hw_config` で探して優先選択する（AV1 の SW フォールバック黒画面対策）
  - `Rendering/`: GPU ゼロコピー描画の中核。`GpuDeviceContext`（描画・デコード共有の単一 D3D11 デバイス）、
    `GpuFrameConverter`（`ID3D11VideoProcessor` による YCbCr→BGRA 変換）、`SwapChainVideoPresenter`
    （映像子ウィンドウのスワップチェーンへ vsync Present）
  - `Video/`: 映像フレームリング。`GpuVideoFrameRing`（GPU 共有テクスチャ）/`VideoFrameRing`（CPU BGRA）と、
    スロット状態機械 `SlotSequencer`（Free/Writing/Ready/Leased・シーク世代・`TryLeaseDue`/`TryLeaseOldest`+
    `ReturnLease` のリース方式）・due 選択 `FrameSelector` に分割されている
  - `SeekEpoch.cs`: シーク世代。`DemuxThread.RequestSeek` が採番し、パケットキューの Flush 番兵・
    パケット・映像リングのスロットに刻まれる。判定は常に等値。レビュー時の観点は
    [.claude/rules/ensemble-review.md](.claude/rules/ensemble-review.md) の
    「6. シーク世代（`SeekEpoch`）の扱い」を参照
  - `Sync/PlaybackClock.cs`: audio-master クロック（mixer 出力サンプル軸のセグメントマップ）。
    `WasapiPositionSource`（`IWavePosition` ベース、異常検知で `FallbackPositionSource` へ自動切替）と組み合わせて
    `MediaEngine.Position` を算出する
  - `Sync/PrerollGate.cs`: シーク後、音声・映像の**両方**のプリロールが終わるまでミキサーの実出力を
    保留する門番。完了通知はシーク世代で等値照合する（採番前は「待つ世代なし」として全て捨てる）。
    待つ世代を確定する順序に制約があり、誤ると保留が永久に解けない。詳細はクラスの doc コメント
  - `Diagnostics/StallDetector.cs`: 「動いているはずのものが動いていない」ことを経過時間で検出する。
    **3 インスタンス**を使う——音声（ミキサーの `Read`）・映像（フレームの提示）・
    クロック（再生位置が進むこと）。例外を伴う異常停止は `WasapiOut.PlaybackStopped` や各スレッドの
    例外記録が拾うが、例外を出さずに黙って止まる経路はここでしか気づけない。判定は
    `MediaEngine.StatusTickCore` の `DetectAudioStall` / `DetectVideoStall` / `DetectClockStall` で、
    いずれも `WriteFatal` で記録する。記録するのは**開始・回復・打ち切りの 3 種**
    （`StallDetector` の `Poll` が局面を返し、`Prime` が打ち切りを申告する。なぜ 3 種必要かは
    両メソッドの doc コメント）。状態は出力が戻れば自動で解ける
    （`IsAudioStalled` / `IsVideoStalled`。開き直しを要する `IsAudioOutputFailed` とは別物）。
    音声・映像が動き続けたままクロックだけが凍る経路があるため 3 つ目が要る。通知は他の検出器と
    重複しないよう選別する。詳細は `DetectClockStall` の doc コメント、レビュー時の観点は
    [.claude/rules/ensemble-review.md](.claude/rules/ensemble-review.md) の
    「7. 条件は『消費側の述語』をそのまま使う」を参照
  - 映像提示は経路で二系統: GPU デコード時は専用 vout スレッドが `SwapChainVideoPresenter` で vsync 直接提示
    する（`IsVideoOutputActive`）。CPU デコード時は従来どおり pull 型で、UI 側の `CompositionTarget.Rendering`
    が毎フレーム `TryGetFrame`/`ReturnFrame` を呼ぶ
- `src/MultiTrackPlayer.UI/`: WPF アプリ・MVVM ViewModel・XAML ビュー。GPU 経路は `Rendering/VideoHwndHost`
  （映像子ウィンドウ）へ直接提示し、airspace で隠れる OSD 等は透過の `Windows/AirspaceOverlayWindow` で重ねる。
  CPU 経路は `Rendering/D3DImagePresenter`（D3D9Ex↔D3D11 共有）で `D3DImage` へ表示する
- `tests/MultiTrackPlayer.Tests/`: xUnit。FFmpeg・D3D11 に依存しないロジックが対象で、
  デコード・描画パイプライン本体は対象外。対象クラスの一覧は
  [.claude/rules/ensemble-review.md](.claude/rules/ensemble-review.md) の
  「5. テスト可能性の設計」を単一の情報源とする（二重管理で食い違わせないため）
- チャプター永続化: `%APPDATA%\MultiTrackPlayer\chapters\{MD5}.json`
- キーバインド設定: `%APPDATA%\MultiTrackPlayer\keybindings.json`
- ファイルオープン（`avformat_open_input`/`avformat_find_stream_info`）は UI スレッドで同期実行される
  （3-4GB ファイルで数百ms かかることがある。非同期化は未対応・既知の TODO）
